import React from 'react';

import './Loader.scss';

const Loader = () => (
  <div className="animation-loader">
    <span>.</span>
    <span>.</span>
    <span>.</span>
  </div>
);

export default Loader;
